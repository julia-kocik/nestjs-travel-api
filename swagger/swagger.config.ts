import { SwaggerConfig } from './swagger.interface';

export const SWAGGER_CONFIG: SwaggerConfig = {
  title: 'Nest js Trips Api',
  description: 'Nest js api allows to create new trips with /all-trips, create account and login with /signup and /signin and save favourite trips after authorization with /favourites.',
  version: '1.0',
  tags: [''],
};
